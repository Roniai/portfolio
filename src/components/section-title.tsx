export const SectionTitle = ({ title }: { title: string }) => {
  return (
    <div className="flex justify-center mb-16">
      <h2 className="text-purple-700 font-bold text-5xl">{title}</h2>
    </div>
  );
};
